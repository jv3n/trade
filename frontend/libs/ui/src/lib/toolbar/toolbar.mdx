import { Canvas, Meta, Source } from '@storybook/addon-docs/blocks';
import * as Stories from './toolbar.stories';

<Meta of={Stories} />

# Toolbar

App-level top bar. Lives inside `<mat-toolbar>` with native flex children — typically a brand on the left, actions on the right via a `flex:1` spacer.

## How to use

<Source
  language="ts"
  code={`import { StbToolbarModule } from '@portfolioai/ui';

@Component({
  imports: [StbToolbarModule],
  template: \`...\`,
})`}
/>

## Default

<Canvas of={Stories.Default} />

