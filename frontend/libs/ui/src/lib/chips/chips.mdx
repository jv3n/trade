import { Canvas, Meta, Source } from '@storybook/addon-docs/blocks';
import * as Stories from './chips.stories';

<Meta of={Stories} />

# Chips

Compact tags — used for asset types, play tags, status markers. `highlighted` for active state.

## How to use

<Source
  language="ts"
  code={`import { StbChipsModule } from '@portfolioai/ui';

@Component({
  imports: [StbChipsModule],
  template: \`...\`,
})`}
/>

## Default

<Canvas of={Stories.Default} />

