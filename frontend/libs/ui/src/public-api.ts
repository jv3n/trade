// Public API of @portfolioai/ui.
//
// The lib is currently **CSS-only** — it ships its design system through `libs/ui/styles/`
// (loaded by the consumer's `styles.scss`) and demos via Storybook stories under
// `libs/ui/src/stories/`. No TypeScript surface to export yet ; add component re-exports here
// when typed components (e.g. UiPage, UiDataTable) graduate from the styles-only approach.
export {};
