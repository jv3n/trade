import { Canvas, Meta, Source } from '@storybook/addon-docs/blocks';
import * as Stories from './icon.stories';

<Meta of={Stories} />

# Icon

Material font icon — **Material Symbols Rounded**, loaded by `styles/_fonts.scss` and registered as the default font set on `MatIconRegistry`. Icon names come from https://fonts.google.com/icons. Colour follows `currentColor`.

## How to use

<Source
  language="ts"
  code={`import { StbIconModule } from '@portfolioai/ui';

@Component({
imports: [StbIconModule],
template: \`...\`,
})`}
/>

## Default

<Canvas of={Stories.Default} />
