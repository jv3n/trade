# État actuel — Phase 2 clôturée (2026-05-06)

Snapshot fin de Phase 2 — tous les items « ticker » de profondeur sont livrés. Le bloc chart analyse v1+v2+v3 (zoom drag-select + brush mini-chart navigator + multi-select overlays MA50/MA200/Bollinger/52w hi-lo + annotations user persistées localStorage + measure tools delta % / delta time) a été complété par : **recommandations analystes** (port `AnalystRecommendationClient` + Finnhub `/stock/recommendation` + `/stock/price-target` + 5e cache `analyst-recommendations`), **earnings dates et derniers résultats** (port `EarningsClient` + `/stock/earnings` + `/calendar/earnings` fail-soft + 6e cache `earnings`), **`instrumentType` end-to-end** (TickerQuote enrichi pour gater le toggle Sector aux STOCK uniquement), **swap sector classifier Twelve Data → Finnhub** (Twelve Data `/profile` est paid-tier only, remplacé par Finnhub `/stock/profile2` sans nouveau runtime key), **sidenav outils chart** (Amazon-style, foldable, accordion sections, embarque la liste annotations posées avec bouton supprimer explicite — clôt en passant le ticket fiabiliser-suppression-annotations), **news inline accordion v1 minimaliste** (chaque headline expand le summary Finnhub + lien externe explicite ; pas de scraping, pas de LLM v1 — décision design honnête). 2ᵉ audit code globale archivé dans `docs/projet/audits/2026-05-06-fin-phase-2.md`, 7 findings Critique+Important ingérés en backlog. **Phase 5 — Déploiement** ouverte au backlog (analyse hébergement + OAuth2 multi-user comme prérequis) ; reste en idée. Pour reprendre proprement à la prochaine session.

## Branches / tags

- Branche : `master`
- Derniers tags :
  - `v0.1.0` — clôture **Phase 0** (recommandations RSS, gelée)
  - `v0.2.0` — clôture **Phase 1 — Pivot ticker** ✅
- Working tree : voir `git status`.

## Phase 1 — bilan

100 % livrée. Tout le critique 🔴, le médium 🟡 et le basse 🟢 listé dans `backlog.md` sont ✅. Prérequis Phase 2 (provider de marché alternatif) également ✅.

## Phase 2 — démarrée

- ✅ **Multi-timeframe + axes + crosshair** : toggle `1D / 5D / 1M / 3M / 1Y / 5Y` au-dessus du chart. Endpoint dédié `/chart?timeframe=` qui ne ramène que les bars (les indicateurs et le narratif restent sur la 1Y daily de référence). Enum `Timeframe` côté domain (intervals Yahoo-style pour aligner les clés Caffeine entre dossier et chart). Mock `MockMarketChartClient` honore `(range, interval)` avec un seed étendu pour produire une courbe différente par timeframe. Chart enrichi axes Y (prix) et X (dates), grille pointillée, crosshair de hover + tooltip date/prix exacts. 4 nouveaux tests slice MVC (`MarketControllerTest`) + spec adapter HTTP + 4 tests `ticker.spec` + 3 tests mock supplémentaires.
- ✅ **Watchlist persistée** : nouveau module backend `watchlist/` (entity `WatchlistEntry`, table `watchlist_entry` migration V3, service avec normalisation symbole + add idempotent + remove non-idempotent, controller 3 endpoints REST). Front : port `WatchlistRepository` + adapter HTTP, **input rapide dans la sidebar dashboard** (section dédiée avec icône poubelle pour retirer) + **bouton "Suivre / Suivi"** sur le header du Dossier ticker (icône `bookmark` filled/outlined, optimistic toggle avec rollback). Pas de gestion multi-user (table sans `user_id`). 14 tests : slice MVC controller (7), adapter HTTP (3), `dashboard.spec` watchlist (6), `ticker.spec` toggle (5).
- ✅ **Sidebar dashboard collapsable + scrollbar custom** : 3 sections indépendamment foldables (Portefeuilles / Tickers détenus / Watchlist) avec bouton header + chevron rotatif. Scrollbar 8px custom appliquée globalement via `styles.scss`, support Webkit + Firefox, adopte les tokens couleur du thème. Pas de persistance localStorage des états ouvert/fermé pour l'instant.
- ✅ **News par ticker** : nouveau module backend `news/` (`NewsClient` port). Deux adapters sélectionnés par `news.provider` : `FinnhubClient` (`finnhub`, /company-news 30j window) et `MockNewsClient` (`mock`, défaut — feed synthétique déterministe par symbole, ~10 % quiet, ~25 % sans summary). Cache `news-by-symbol` 15 min. Endpoint `GET /api/market/ticker/{symbol}/news?limit=10`. Front : port `NewsRepository` + adapter, section dédiée sur le Dossier ticker entre la plage 52w et le narratif IA, dates relatives localisées (`il y a 3 h` / `hier` / `15 avr 2026`), erreurs scopées. Twelve Data ne couvrant pas `/news` (testé live → 404), Finnhub est ajouté comme provider séparé. Mock par défaut en local pour économiser le quota Finnhub en itération. 17 tests.
- ✅ **Settings & config runtime** : nouvelle page `/settings/configuration` (4e onglet sidenav) qui édite en direct **sept clés** sans reboot — les deux clés API Twelve Data + Finnhub (avec bouton "Tester" qui sonde la clé candidate avant la sauve), le TTL cache Caffeine 5–60 min, et les **quatre toggles provider** mock ↔ live (`market.provider` / `news.provider` / `analyst.provider` / `earnings.provider`). Backend : nouveau module `config/` (`AppConfigService` + cache mémoire `ConcurrentHashMap` primé au boot + surcharge BDD au-dessus du défaut YAML + `ConfigChangedEvent`), migration V4 `app_config (key/value/updated_at)`. Routing per-call : `RoutingMarketChartClient` + `RoutingNewsClient` + `RoutingAnalystClient` + `RoutingEarningsClient` (tous `@Primary`) délèguent à l'adapter sélectionné par `appConfig.getString(...)` à chaque appel. TTL Caffeine dynamique via `CacheTtlListener` qui rebuild la spec sur event. UI Material avec `mat-button-toggle-group` pour les enums (save instantané) + slider pour le TTL + password inputs pour les secrets. Hint conditionnel sur la card Twelve Data quand la clé Finnhub est absente — signale que le Sector benchmark utilise Finnhub même en mode market `twelvedata` (audit Phase 2 finding #4). ~30 tests (`AppConfigServiceTest`, `ConfigControllerTest`, `RoutingMarketChartClientTest`, `RoutingNewsClientTest`, `configuration.spec`, `config.http.spec`).
- ✅ **Lifecycle de position OPEN/CLOSED dans l'import CSV** : bug observé — un ticker vendu (e.g. XAU dans l'export Wealthsimple suivant) restait visible dans le portfolio "live" parce que `CsvImportService.import` faisait un upsert pur sans cleanup. **Fix V5** : ajout d'un lifecycle explicite sur la table `asset` (`status` ∈ `{OPEN, CLOSED}`, `opened_at`, `closed_at`). Au lieu de hard-delete on flippe en CLOSED, valeurs figées à la dernière snapshot connue. Comportement import : par account, après upsert, identifier `to-close = existing OPEN − csvTickers` → flip CLOSED ; un asset CLOSED qui réapparaît dans le CSV → reopen. Query `findByPortfolioIdAndStatus(OPEN)` côté `PortfolioQueryService.findAssets`. UI : compteurs "N positions fermées / réouvertes" surfacés dans la confirmation d'import (single + batch agrégé). Snapshot Suivi inchangé — il capture toujours la vérité par batch indépendamment du status. Future page "Positions historiques" (Phase 4) lira les CLOSED. 3 tests Mockito-Kotlin sur le lifecycle (close, reopen, no-op).
- ✅ **Watchlist v2 — autocomplete + validation des tickers** : bug d'origine — l'input watchlist acceptait n'importe quelle chaîne (10 caractères max), 404 ne tombait qu'à l'ouverture du dossier. v2 livrée : nouveau port backend `SymbolSearchClient` avec deux adapters (`TwelveDataSymbolSearchClient` REST `/symbol_search` 1 credit/call + `MockSymbolSearchClient` ~30 symbols US/TSX seedés), `RoutingSymbolSearchClient` `@Primary`, `SymbolSearchService` avec `@Cacheable("symbol-search")` + `validate(symbol)` exact case-insensitive. Endpoint `GET /api/market/symbols/search?q=&limit=`. `WatchlistService.add` branche `symbolSearch.validate(...)` après le check d'idempotence (skip la revalidation pour épargner un credit) — rejet → 400 "not recognised". Front : `MarketRepository.searchSymbols(query, limit)`, sidebar dashboard refacto avec `mat-autocomplete` debounce 300 ms, `FormControl<string | SymbolMatch | null>`, dropdown `<strong>SYMBOL</strong> — Name (EXCHANGE)`, bouton "+" disabled tant que pas de match. 503 sur la search → dropdown collapse silencieusement. Pas d'override "j'insiste" v1. 10 tests backend + 9 frontend.
- ✅ **Comparaison vs benchmark v1** : overlay opt-in d'un indice (SPY / QQQ / IWM) sur le chart du dossier ticker. **Front-only, zéro changement back** — réutilise `GET /api/market/ticker/{symbol}/chart?timeframe=` pour fetch le benchmark comme un ticker normal (cache Caffeine `market-chart` partagé sur la clé `(symbol, range, interval)` → tous les dossiers ouverts sur le même benchmark se partagent l'entrée 15 min). UX : `mat-button-toggle-group` Off/SPY/QQQ/IWM dans la chart-toolbar (Off par défaut → zéro credit Twelve Data tant qu'aucun click), Y-axis flippe instantanément du prix absolu vers le % return signé depuis la 1ère bar quand un benchmark est actif (sinon SPY à 500 $ écraserait visuellement un small-cap à 15 $), 2ᵉ ligne dashed semi-transparente (`--color-text-dim`, opacity 0.75) qui apparaît dès résolution du fetch. Tooltip enrichi avec swatch + symbol + valeur par série. `MatTooltipModule` ajouté pour expliciter les indices ("S&P 500 (SPY)") via tooltip + aria-label. Geometry : `chartGeometry` computed devient bi-mode, alignement par index (assumé OK pour benchmarks US sur tickers US ; truncate silent à la longueur commune pour dual-listed comme RY.TO). Cohérence multi-timeframe : `selectTimeframe` refetch les deux séries en parallèle. Erreur scoping : 503 sur le benchmark surface un banner inline `ticker.benchmark.errors.fetch` mais ne touche pas `chartBars` ni `snapshot()`. 6 tests `ticker.spec.ts` (default off / fetch on toggle / no-op re-click / sync timeframe / error scope / off clears). i18n FR + EN.
- ✅ **Comparaison vs benchmark v2 — Sector + Custom** : étend l'overlay v1 avec deux nouveaux modes. **Sector** auto-détecte le SPDR sector ETF qui couvre le secteur GICS du ticker (AAPL → XLK Technology, JPM → XLF Financials, etc.) ; **Custom** laisse l'utilisateur taper n'importe quel ticker via un `mat-autocomplete` sidecar dans la toolbar (réutilise `searchSymbols`). Backend : nouveau port `SectorClassifier` avec deux adapters — `FinnhubSectorClassifier` (REST `/stock/profile2`, free tier 60 calls/min, parse `finnhubIndustry` + route via `SpdrSectorEtfs` ; **remplace `TwelveDataSectorClassifier` depuis 2026-05-06** parce que Twelve Data `/profile` est paid-tier only sur les comptes free) et `MockSectorClassifier` (~25 tickers seedés). `RoutingSectorClassifier` (`@Primary`) — route `mock` → mock, `twelvedata` → Finnhub (asymétrie cachée derrière le routing). `SpdrSectorEtfs` (`internal object`) hardcode les 11 SPDR sectors (Technology→XLK, Financials→XLF, Healthcare→XLV, Energy→XLE, Consumer Discretionary→XLY, Consumer Staples→XLP, Communication Services→XLC, Industrials→XLI, Materials→XLB, Real Estate→XLRE, Utilities→XLU) + table de synonymes ("Information Technology" / "Health Care" / "Consumer Cyclical"). `SectorClassifierService` avec `@Cacheable("sector-by-symbol")` (clé SpEL `#symbol.trim().toUpperCase()`). Endpoint `GET /api/market/ticker/{symbol}/sector-benchmark` retournant `{tickerSymbol, sector, etfSymbol, etfName}`. UI : toggle 5-buttons (Off/SPY/QQQ/IWM/Sector) + autocomplete sidecar "or pick…". Sector hors SPDR → 404 → message inline distinct (`sectorNotMapped`). Front : refacto signaux `resolvedBenchmark: {symbol, label}` (source de vérité geometry + legend) et `customBenchmarkControl` (FormControl). `selectTimeframe` utilise `resolvedBenchmark()?.symbol` → un changement de timeframe avec sector actif refetch l'ETF résolu sans re-résoudre (épargne 1 credit Twelve Data par click). 35 tests backend (`MockSectorClassifierTest` 9, `SpdrSectorEtfsTest` 9, `TwelveDataSectorClassifierTest` 10, `RoutingSectorClassifierTest` 3, `MarketControllerTest` extension 4) + 5 frontend dans `ticker.spec.ts`. i18n FR + EN.
- ✅ **Chart : analyse + sélection v1+v2+v3** : extension du chart Dossier ticker avec trois couches d'outils interactifs. **v1 zoom drag-select** : `pointerdown` → drag horizontal → `pointerup` au-delà de 10 px commit un zoom (slice symétrique des séries ticker + benchmark dans `chartGeometry`), reset via bouton toolbar visible quand zoomé ou double-clic. `selectTimeframe` clear le zoom (les indices ne traversent pas les timeframes). `pointerCapture` sur la chart-canvas pour survivre à une sortie temporaire du curseur. **v2 overlays multi-select** : `mat-button-toggle-group` MA50 / MA200 / Bollinger / 52w hi / 52w lo, calculé front-side depuis la **série complète** (pas la fenêtre zoomée) puis sliced à l'affichage — un zoom 30 bars conserve la sémantique "MA50 de la série complète" au lieu de produire 30 nulls. `rollingMean` linéaire, `bollinger` naïf O(n²) sur n=20. 52w hi/lo lus de `snapshot.quote.fiftyTwoWeekHigh/Low`. Désactivé en mode benchmark (Y axis en % return space). **v3 brush mini-chart** : SVG 52 px de haut sous le main, mirroir de la full series avec rectangle draggable indiquant la zone zoomée — trois modes selon la zone du grab (`pan` / `resize-left` / `resize-right`, hit-zone 8 px), reset si click hors rectangle, `BRUSH_MIN_BARS = 2` empêche de collapser le rectangle. **v3 annotations** : nouveau port frontend `AnnotationRepository` (`core/annotation.repository.ts`, 9ᵉ repository) + adapter `LocalStorageAnnotationRepository` (`core/adapters/annotation.local.ts`, key `ticker-annotations:{SYMBOL}`, `crypto.randomUUID` avec fallback, `defer()` pour convertir un throw quota-exceeded en error notification). Bouton toolbar "+ Annotation" arme le mode ; le prochain click sub-threshold pose une h-line au prix cliqué (inverse-yAt via `geom.yMin` + `yRange`). Out-of-range → clamp top/bottom + suffixe ↑/↓. Click sur le handle `×` → optimistic remove avec rollback, keyboard activation Enter + Space (ARIA `role="button"`). Wiring `app.config.ts`. Délégation à un futur backend-backed adapter sans rewrite UI. **v3 measure tools** : un click sub-threshold (sans annotation mode) pose un anchor au bar le plus proche, hover montre delta % et delta time (`formatDeltaTime` adaptatif min/h/j) dans une ligne distincte du tooltip. Anchor recovered par timestamp à chaque geometry pass — robuste au zoom qui shift les indices. `selectTimeframe` et `resetZoom` clear l'anchor. Désactivé en benchmark mode. **Click vs drag distinguishment** partagé (threshold 10 px) entre les 3 features ; `onChartPointerDown` skip si `event.target.closest('.annotation-delete')` pour ne pas voler le click au handle de suppression. **Tests** : 19 nouveaux dans `ticker.spec.ts` (8 v1+v2 dont un test régression "MA50 stays computed on the full series even when zoomed", 9 v3, 1 keyboard activation handler, 1 zoom commit). i18n FR + EN (zoom, overlays, annotation, measure, brush).
- ✅ **Outillage no-wildcard imports** (Phase 2.5) : `.editorconfig` racine bloque IntelliJ d'introduire des wildcards via "Optimize Imports" (`ij_kotlin_name_count_to_use_star_import = MAX`). Custom step Spotless `no-wildcard-imports` dans `backend/build.gradle.kts` qui scanne et lance `GradleException` sur tout wildcard hors allowlist (14 packages tolérés à shrinker progressivement). Detekt rule `WildcardImport` désactivée — Spotless casse le build, Detekt ne ferait que rapporter. Volontairement pas de ktlint après un faux départ douloureux (152 fichiers reformatés en consolidation `*` parce que ktlint applique la sémantique IntelliJ de `ij_kotlin_packages_to_use_import_on_demand` = "force ces packages en wildcard" au lieu de "tolère"). Règle ajoutée à `CLAUDE.md`. Item dette technique « Shrink l'allowlist » créé pour le suivi.
- ✅ **Cleanup des jobs orphelins au boot** : `OrphanedJobCleanupListener` (`@EventListener(ApplicationReadyEvent)`) sweep `ticker_narrative_job` + `analysis_job` au boot, flippe `PENDING → ERROR` avec un marqueur. JPQL `@Modifying` bulk update.
- ✅ **Linter ESLint côté frontend** : flat config `eslint.config.js` (Angular ESLint 21) via `ng add @angular-eslint/schematics`, `eslint-config-prettier` en dernier extends. Step CI `Lint` ajoutée à `frontend.yml` avant le build. Premier pass = 21 erreurs traitées dans le même commit (a11y `(keydown.enter)` + `role="button"` sur les divs cliquables, `prefer-inject` migré, labels mal-placés convertis en `<span class="filter-label">`, ternaires en statement transformés en `if/else`, imports non utilisés supprimés).
- ✅ **Doc-maintainer subagent** : `.claude/agents/doc-maintainer.md` (Read/Glob/Grep, no Edit, no Bash) + slash command `/doc-maintainer` qui spawne un audit en contexte isolé. Trois capacités : cross-check factuel, ton, cross-link. Rendu en punch-list HIGH/MED/LOW.

### Backend

- Module `market/` : port `MarketChartClient` qui retourne un `MarketChart` (types domaine `TickerQuote` + `List<OhlcBar>`). Deux adapters :
  - `TwelveDataClient` (REST + apikey, défaut prod) — deux endpoints `/time_series` + `/quote`, cache Caffeine 15 min, parser tolérant aux quirks (numériques en strings, erreurs HTTP 200 avec `status: error`), timeouts connect 5 s + read 10 s.
  - `MockMarketChartClient` (synthétique déterministe par symbole, défaut sans clé pour CI / onboarding). Symboles réservés `UNKNOWN` (404) et `RATELIMIT` (503).
- `IndicatorCalculator` Kotlin pur, 20+ tests.
- Pipeline narratif LLM async : `Service → Runner @Async → Executor (parse + validate + 1 retry) → Persister`. Cache snapshot 30 min, dedup job 5 min. Validateur strict : 3-5 keyPoints, ≤15 mots, summary 2-3 phrases, sentiment ∈ enum.
- Module `watchlist/` (Phase 2) : entity `WatchlistEntry`, service avec normalisation + add idempotent + remove 404 si absent, controller 3 endpoints REST.
- Module `config/` (Phase 2) : `AppConfigService` (read layered YAML/BDD, write-through + `ConfigChangedEvent`), `ConfigController` (CRUD + endpoints `/test/{provider}` qui sondent une clé candidate sans la sauver), `ConfigTestClient` (RestClient dédié), `RoutingMarketChartClient` + `RoutingNewsClient` (`@Primary`) délèguent par appel, `CacheTtlListener` rebuild la spec Caffeine sur event TTL.
- Listener `OrphanedJobCleanupListener` (au boot, `ApplicationReadyEvent`) : flip tous les `PENDING` en `ERROR` sur `ticker_narrative_job` + `analysis_job`.
- Migrations Flyway : V1 init, V2 ticker_narrative, V3 watchlist_entry, V4 app_config, V5 asset_lifecycle.
- Endpoints : `GET /api/market/ticker/{symbol}` (dossier complet), `GET .../chart?timeframe=` (bars only multi-timeframe), `POST/GET /narrative/...` (kick + poll + latest), `GET /narrative/preview` (preview prompt), `GET /api/market/symbols/search?q=&limit=` (autocomplete watchlist), `GET .../news?limit=` (Phase 2 news), `GET /api/portfolios/owned-tickers`, `GET POST DELETE /api/watchlist[/symbol]`, `GET PUT DELETE /api/config[/key]`, `POST /api/config/test/{provider}`.

### Frontend

- Page Dossier ticker : graphe SVG inline avec **toggle multi-timeframe** (`1D / 5D / 1M / 3M / 1Y / 5Y`), **axes prix + dates** + grille pointillée, **crosshair au survol** + tooltip date/prix, **overlay benchmark opt-in** (`Off / SPY / QQQ / IWM / Sector / Custom` avec Y-axis bi-mode prix/% return + 2ᵉ polyline dashed + tooltip enrichi à deux séries), **chart analyse interactive** (zoom drag-select avec brush mini-chart navigator en bas, multi-select overlays MA50/MA200/Bollinger/52w hi-lo, h-line annotations user persistées localStorage par symbole, measure tools delta %/delta time entre deux clics), 10 chips d'indicateurs avec color-coding (RSI/MA/perf/drawdown), **bouton "Suivre / Suivi"** (watchlist toggle), section news (10 headlines + dates relatives), narratif IA (sentiment chip BULLISH/NEUTRAL/BEARISH coloré, summary, bullets, footer modèle+date), bouton Régénérer avec polling.
- Dashboard : sidebar **3 sections collapsables** (Portefeuilles / Tickers détenus / Watchlist) avec total agrégé tous portefeuilles, liste cliquable des tickers détenus (`owned-tickers` agrégé serveur, pas de N+1), **`mat-autocomplete` watchlist** (debounce 300 ms sur `/symbols/search`, dropdown `<strong>SYMBOL</strong> — Name (EXCHANGE)`, "+" disabled tant que pas de match) + liste cliquable + icône poubelle. Scrollbar custom 8px.
- Settings adaptés Phase 1 : `prompt-preview` par ticker (input libre + suggestions), `test-sources` étendu avec test ticker.
- **i18n FR/EN** via `ngx-translate` (TranslatePipe) + `LanguageService` signal-based. Drapeaux unicode dans le header.
- **Zoneless explicite** (`provideZonelessChangeDetection()` dans `app.config.ts`).

### Tests

- Backend : `IndicatorCalculatorTest` (20+), `MockMarketChartClientTest` (9, ajouts intraday/weekly/seed), `TwelveDataClientTest` (9), `TwelveDataMappersTest` (5), `MarketControllerTest` (5, slice MVC chart endpoint), `TickerNarrativeServiceTest` (8), `TickerNarrativePrompt/Parser/Validator` (17), `TickerNarrativePreviewControllerTest` (2), `PortfolioControllerTest` enrichi (owned-tickers), `WatchlistControllerTest` (7).
- Frontend : adapters HTTP (incl. `watchlist.http.spec`), ticker page (incl. timeframe toggle + watchlist), dashboard (incl. owned tickers + watchlist add/remove/rollback), suivi, csv-import, narrative flow complet.

### Décisions techniques notables (consolidées)

- **Provider primaire = Twelve Data**. Yahoo a été tenté (cookie+crumb dance complet) puis supprimé : ban IP résidentielles + VPN + cellulaire, pas viable pour un projet perso à IP unique. Code Yahoo consultable dans l'historique git (commit `b993440`) si besoin de rejouer.
- **Twelve Data quirks** absorbés par le client : numériques en strings JSON (toBigDecimalOrNull tolère ""/NaN), erreurs en HTTP 200 avec `status: error` (parser inspecte le body), code 404/429/401 mappés respectivement vers `NoSuchElementException` / `MarketUnavailableException("rate-limited")` / `auth-failed`. Clé API absente → exception explicite avant l'appel HTTP.
- **Port en types domaine** (`MarketChart` au lieu de la forme upstream brute) — un provider supplémentaire = un nouvel adapter, zero churn ailleurs.
- **HTTP timeouts** sur `TwelveDataHttpConfig` (connect 5 s, read 10 s) via `JdkClientHttpRequestFactory` pour éviter de hanger un thread Tomcat sur DNS / TLS lent.
- **CI cache 3-tier via `gradle/actions/setup-gradle@v6`** (backend.yml + codeql.yml) : dependency / build / configuration caches gérés par l'action officielle Gradle, mode `cache-read-only` sur les PRs pour éviter le thrash, build cache partagé entre `backend.yml` et `codeql.yml` sur master. Frontend ne cache plus que les deps npm via `actions/setup-node@v6` (le cache `.angular/cache` avait été tenté puis retiré — non alimenté en mode prod). CodeQL active `trap-caching: true` (cache la BDD extraite par langage, ~2 min cold → 30 s incrémental).
- **Detekt tuning pragmatique** (217 → 26 findings) — exclusions ciblées sur `LongParameterList` pour `@Entity` / `@Embeddable`, `MagicNumber` whitelist HTTP / percent / timeouts + excludes Mock + IndicatorCalculator, `WildcardImport` autorise jakarta/spring/junit, `TooGenericExceptionCaught` exclut adapters HTTP. `ignoreFailures = true` pour l'instant — bascule à `false` quand on aura un baseline propre. Workaround Kotlin 2.1 : `resolutionStrategy` pin la classpath Detekt sur Kotlin 2.0.10 (Detekt 1.23.7 compilé contre 2.0.10).
- **Doc ops centralisée** (`docs/technique/ops.md`) — workflows GitHub Actions, stratégie de cache, permissions GITHUB_TOKEN minimales par workflow, Code Scanning (CodeQL + Detekt SARIF), Dependabot, troubleshooting. Cross-référencée depuis `architecture.md`, `developper.md`, `developpement.md` et ajoutée à la nav mkdocs avec `providers.md`.

## Reprise possible — par ordre d'utilité

### Dette technique (cf. `backlog.md`)

A. **Items de l'audit 2026-05-02 non fixés** : contrat preview CSV cassé (front lit `bookValue`, back envoie `bookValueCad`), `@EnableAsync` sans `ThreadPoolTaskExecutor`, N+1 sur la timeline snapshots.

B. **`provideRepositories()` côté frontend** 🟢 — extraire les 9 lignes répétitives `{ provide: XxxRepository, useClass: <impl> }` de `app.config.ts` dans `core/providers.ts` via `makeEnvironmentProviders([...])`. ~15 min.

### Phase 2 — restant à attaquer (clôture après ces 3 items)

Le backlog Phase 2 a été réduit aux items strictement ticker pour permettre de clôturer la phase à leur livraison. Les autres ont migré vers la nouvelle **Phase 2.5 — Stabilisation et outils**.

- **Recommandations analystes** — consensus + target prices via Twelve Data `/analyst_estimates` ou Finnhub `/stock/recommendation` (à arbitrer au moment de l'attaque)
- **Earnings dates et derniers résultats** — Finnhub `/stock/earnings`, encart fundamentals partagé avec recos analystes
- **News inline** — accordéon de contenu sur les headlines sans navigation externe (à coupler avec Phase 3 observabilité narrative)

### Phase 2.5 — Stabilisation et outils (nouvelle, après réorg backlog)

- **Config runtime v2 : LLM provider + model** 🟡 Moyenne — extension `/settings/configuration` avec `llm.provider` (claude/ollama), `ollama.model`, `anthropic.api.model`. Nouveau `RoutingLlmClient` `@Primary` + retrait des `@ConditionalOnProperty` sur `ClaudeClient`/`OllamaClient`. Bouton "Tester" qui mesure latence + parse correctness sur un mini prompt fixe.
- **Drag-drop portfolios sidebar** 🟢 Basse — Angular CDK `DragDropModule`, persistance `localStorage portfolio-order`.
- **Sidebar modulaire** 🟢 Basse — chaque section devient un module détachable façon Bloomberg terminal. Utile une fois 5-6+ modules.

### Dette ouverte saillante (cf. `backlog.md`)

- Shrink progressif de l'allowlist `no-wildcard-imports` Spotless (14 packages → idéalement 4-5 idiomes purs)
- Sweep `::ng-deep` côté frontend — quand Material 22+ exposera mieux la density API
- Coutures benchmark v2 (double normalisation `trim().uppercase()`, reset `customBenchmarkSearching` flag, blinder `displayCustomBenchmark`)
- Agent code-reviewer pré-commit (à l'image de `doc-maintainer`)
